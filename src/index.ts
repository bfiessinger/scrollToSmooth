/**
 * Package entry point.
 *
 * Re-exports the ScrollToSmooth class (as default and named),
 * all type aliases, and every built-in easing function.
 */
export { ScrollToSmooth, ScrollToSmooth as default } from './scrolltosmooth';
export type { Options, ScrollData, ScrollUpdateData, EasingFunction } from './scrolltosmooth';
export * from './easings';
